import { AddshadowDirective } from './addshadow.directive';

describe('AddshadowDirective', () => {
  it('should create an instance', () => {
    const directive = new AddshadowDirective();
    expect(directive).toBeTruthy();
  });
});
