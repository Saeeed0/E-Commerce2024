import { CridetCardPipe } from './cridet-card.pipe';

describe('CridetCardPipe', () => {
  it('create an instance', () => {
    const pipe = new CridetCardPipe();
    expect(pipe).toBeTruthy();
  });
});
